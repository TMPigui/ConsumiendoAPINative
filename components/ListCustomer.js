import { View,Text } from "react-native-web";
import { styles } from "../assets/styles/styles";

export default function ListCustomer(){
    return(
        <view style={styles.container}>
            <Text>Estamos en clienntes</Text>
        </view>
    )
}